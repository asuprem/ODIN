from setuptools import setup

setup(name="mlep", 
        version="0.1",
        description="ml drift adaptive event processor",
        author="Abhijit Suprem",
        packages=["mlep"],
        zip_safe=False)